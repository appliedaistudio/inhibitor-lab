suites:
  - id: runtime_trajectories
    name: Runtime Trajectories
    path: benchmarks/core/runtime_trajectories
    category: core
    support_level:
      - native
      - compatibility_mapped
      - simulated
    required: true
    status: implemented
    description: "Active benchmark suite combining live signal evidence with deterministic mapping and simulated no-side-effect execution."
  - id: operational_stress
    name: Operational Stress
    path: benchmarks/diagnostics
    category: diagnostic
    support_level: native
    required: false
    status: planned
    description: "Diagnostics-only area; not publication evidence or active runtime-trajectory results."
