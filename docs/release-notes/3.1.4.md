# Inhibitor Service 3.1.4

## Consistent rule testing key

- Updated the indirect prompt injection notebook to use one API key for rule generation, rule listing, and test requests.
- This makes sure the notebook tests the rules created for that key.
- Rule generation remains off by default and requires the key to have the needed rule permissions.
